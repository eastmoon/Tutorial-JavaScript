/**
 * Created by EastMoon on 2014/8/23.
 */
$(document).ready(Initial);
$(window).resize(Update);

    function Initial()
    {
        // Initial interaction
        // Initial View
        Update();
    }

    function Update()
    {
    }